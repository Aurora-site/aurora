---
import { getImage, Image } from "astro:assets";
import auroraback_mb from "../../assets/auroraback_mb.png";
const auroraBackMbImageUrl = `url(${(await getImage({ src: auroraback_mb })).src})`;
import auroraback from "../../assets/auroraback.png";
const auroraBackImageUrl = `url(${(await getImage({ src: auroraback })).src})`;
import spb1 from "../../assets/aurora-spb_1.png";
import spb2 from "../../assets/aurora-spb_2.png";
import spb3 from "../../assets/aurora-spb_3.png";
---

<style define:vars={{ auroraBackMbImageUrl, auroraBackImageUrl }}>
  #description {
    background-image: var(--auroraBackMbImageUrl);
  }
  @media (min-width: 768px) {
    #description {
      background-image: var(--auroraBackImageUrl);
    }
  }

  img {
    max-width: 100%;
    height: 100%;
    border-radius: 20px;
  }

  h2 {
    margin-top: 40px;
    margin-bottom: 10px;
    font-size: 24px;
  }
  p {
    text-align: justify;
  }
  ul {
    display: block;
    text-align: left;
    list-style: disc;
    padding-left: 20px;
  }
  ol {
    display: block;
    text-align: left;
    list-style: decimal;
    padding-left: 20px;
  }
  h1 {
    font-size: 64px;
  }

  .description {
    align-items: center;
    text-align: center;
  }

  .description h2 {
    font-family: "SongerGrotesqueBold";
    font-size: 24px;
    margin: 60px 0 85px 0;
  }

  .images {
    display: flex;
    gap: 30px;
    overflow-x: auto;
    scroll-behavior: smooth;
    margin: 10px 0 0 0;
    white-space: nowrap;
    justify-content: space-between;
    padding-bottom: 100px;
  }

  .image-slider-container {
    position: relative;
    margin: 0 auto;
    overflow: visible;
  }

  .imag {
    flex: 0 0 auto;
    width: 360px;
    position: relative;
    filter: drop-shadow(10px 10px 10px rgba(0, 0, 0, 0.8));
    border-radius: 20px;
  }

  .slider-button {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    /* background: rgba(0, 0, 0, 0.5); /* Полупрозрачный фон */
    color: black;
    border: none;
    cursor: pointer;
    padding: 5px 15px;
    z-index: 1000;
    font-size: 30px;
  }

  .slider-button.left {
    left: -50px;
    color: white;
  }

  .slider-button.right {
    right: -50px;
    color: white;
  }

  @media (max-width: 510px) {
    .description h1 {
      line-height: 68px;
      font-size: 52px;
    }
  }

  @media (max-width: 1300px) {
    .images {
      overflow-x: auto;
    }
  }

  @media (max-width: 952px) {
    .slider-button {
      display: none !important;
    }
  }
  @media (max-width: 482px) {
    .imag {
      width: 90%;
    }
  }
</style>

<section
  class="description bg-contain bg-[center_50px] bg-no-repeat pt-[80px] pb-5 text-center md:bg-[0px_20px]"
  id="description"
>
  <h1>Северное сияние в Санкт-Петербурге</h1>
  <h2>Блог о северном сиянии</h2>
  <p>
    Можно ли увидеть северное сияние в Санкт-Петербурге и что для этого нужно?
  </p>
  <p>Короткий ответ: да, можно. Но давайте разбираться.</p>
</section>

<div class="pt-[25px] pb-[50px]">
  <h2>Северное сияние в СПб</h2>
  <p>
    Санкт-Петербург находится на широте 59.93° N, 30.31° E. Обычно северное
    сияние (в нашем случае — полярное сияние) видно на широтах около и выше
    Полярного круга.
  </p>
  <p>&nbsp;</p>
  <p>
    Для того чтобы сияние было заметно в Санкт-Петербурге и около него,
    геомагнитная активность должна быть высокой — с индексом Kp около 6 и выше.
  </p>
  <p>
    За индексом можно следить <a href="https://polarlights.pro/#index"
      ><u>здесь</u></a
    >
  </p>
  <p>&nbsp;</p>
  <p>Прогноз на 27 дней обновляется раз в неделю — обычно по понедельникам.</p>
  <p>Прогноз на 3 дня обычно обновляется ежедневно.</p>
  <p>
    Конечно, лучше ориентироваться на краткосрочный прогноз и помнить, что
    индекс, как и погода, очень изменчив.
  </p>
  <p>&nbsp;</p>
  <p>
    В 2024 и 2025 годах, а по прогнозам и в 2026 году, Солнце радует охотников
    за сиянием сильными вспышками, и мы всё чаще наблюдаем сияние в широтах,
    значительно южнее Заполярья — в том числе и в Санкт-Петербурге.
  </p>

  <h2>Советы по наблюдению от Поларлайтс</h2>
  <ol>
    <li>
      <a href="https://polarlights.pro/#index"><u>Следите за индексом</u></a>;
    </li>
    <li>
      <a href="https://polarlights.pro/#map"><u>Проверяйте вероятность</u></a> в
      вашей геолокации и на карте в целом;
    </li>
    <li>
      <a href="https://polarlights.pro/#map"><u>Учитывайте облачность</u></a> — через
      плотные облака сияние не видно;
    </li>
    <li>
      <a href="https://polarlights.pro/"><u>Используйте Поларлайтс</u></a>.
    </li>
  </ol>

  <h2>И будет вам сияние!</h2>

  <div class="mt-[60px] mb-[50px] text-left">
    <a
      class="font-[SongerGrotesqueBold] text-[24px] font-bold underline underline-offset-2"
      href="/blog">В блог о северном сиянии</a
    >
  </div>
  <div class="pb-[50px] text-left">
    <a
      class="font-[SongerGrotesqueBold] text-[24px] font-bold underline underline-offset-2"
      href="/">Перейти на главную страницу с картой и вероятностью</a
    >
  </div>

  <div class="image-slider-container">
    <button class="slider-button left" id="slideLeft">&#8249;</button>
    <div class="images" id="slider">
      <div class="imag">
        <Image src={spb1} alt="Северное Сияние" />
      </div>
      <div class="imag">
        <Image src={spb2} alt="Северное Сияние" />
      </div>
      <div class="imag">
        <Image src={spb3} alt="Северное Сияние" />
      </div>
    </div>
    <button class="slider-button right" id="slideRight">&#8250;</button>
  </div>

  <script is:inline>
    {
      function initSlider() {
        const slider = document.getElementById("slider");
        const slideLeft = document.getElementById("slideLeft");
        const slideRight = document.getElementById("slideRight");

        if (!slider) return;

        const totalWidth = Array.from(slider.children).reduce(
          (sum, item) => sum + item.offsetWidth,
          0,
        );
        const visibleWidth = slider.offsetWidth;

        if (totalWidth > visibleWidth) {
          slideLeft.style.display = "block";
          slideRight.style.display = "block";

          slideLeft.addEventListener("click", () => {
            slider.scrollBy({ left: -380, behavior: "smooth" });
          });

          slideRight.addEventListener("click", () => {
            slider.scrollBy({ left: 380, behavior: "smooth" });
          });
        } else {
          slideLeft.style.display = "none";
          slideRight.style.display = "none";
        }
      }

      window.addEventListener("load", initSlider);
      window.addEventListener("resize", initSlider);
    }
  </script>
</div>
